# Logistic Regression

I implemented binary logistic regression and trained it with gradient descent
on a small separable dataset. The example exposes the sigmoid, error, and
weight-update steps directly.

```bash
python3 logistic_regression.py
```

I would add a train/test split, regularisation, and precision/recall reporting
before applying it to a real dataset.
