# Blocked Matrix Multiply

A dependency-free Python reference implementation of tiled matrix
multiplication. The tile size is configurable so cache behaviour can be
explored experimentally.

```bash
python3 matmul.py --size 32 --tile 8
```
