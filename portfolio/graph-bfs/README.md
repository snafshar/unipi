# Graph BFS

I implemented breadth-first search over an adjacency-list graph. The program
finds a shortest unweighted path and reconstructs the route from the target
back to the source.

```bash
python3 graph_bfs.py
```

I chose BFS because it gives me a clear correctness baseline before exploring
parallel graph traversal or weighted shortest-path algorithms.
